# include <stdio.h>

int main(){

    //vars 
    //char stores a single character. use '' 
    
    char grade = 'F';
    char symbol = '!';
    char currency = '$';

    printf("Your grade is %c\n", grade);
    printf("Your favorite symbol is %c\n", symbol);
    printf("The currency is %c", currency);

    return 0;
}